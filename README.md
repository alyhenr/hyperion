# hyperion
Ultra-low latency, deterministic matching engine. It will not rely on the operating system for anything during the critical path. No runtime memory allocation, no system calls, no lock contention, and zero pointer chasing.
